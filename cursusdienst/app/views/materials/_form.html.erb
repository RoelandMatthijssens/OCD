<%= form_for(@material) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>

  <div class='field'>
    <%= f.label :name %> <br>
    <%= f.text_field :name %> <br>
  </div>
  <div class='field'>
     <%= render "subject_field", :f => f if @material.subject%>

    <div class="field" id="add_subject_link" <%= "style='display: none;'" if @material.subject  %> >
      <%= link_to_create_hide_items "add to subject", :subject, ["add_subject_link"],f %>
    </div>
  </div>


  <div class="actions">
    <%= f.submit @submit %>
  </div>
<% end %>