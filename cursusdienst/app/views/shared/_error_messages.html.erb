<%#TODO change language errors%>
<% if object.errors.any? %>
  <div id="error_explanation">
    <h2><%=  t(:error, :scope => 'errors') %></h2>
    <p><%= t(:problems, :scope => 'errors') %></p>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
