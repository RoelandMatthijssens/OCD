<h1><%= t(:all_disciplines, :scope => "titles" ) %></h1>
<%= will_paginate %>
<%= link_to t(:add_discipline, :scope => "links" ), new_discipline_path %>
<ul class="disciplines">
  <% @disciplines.each do |discipline| %>
    <%= render discipline %>
  <% end %>
</ul>
<%= will_paginate %>

