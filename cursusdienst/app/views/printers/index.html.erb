<h1><%= @title %></h1>
<% if current_user.can? "create_printers" %>
  <p class="comment_text" ><%= link_to t(:new_printer, :scope => "links" ), new_printer_path %></p>
<% end %>
<div class="printers">
  <% @printers.each do |printer| %>
    <%= render printer %>
  <% end %>
</div>
