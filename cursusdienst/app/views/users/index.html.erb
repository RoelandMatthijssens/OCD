<h1><%= t(:all_users,:scope => "titles") %></h1>
<%= link_to t(:add_user,:scope => "links"), new_user_path %><br/>
<%= render :partial => 'pages/filters' %>
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>
<%= will_paginate %>
