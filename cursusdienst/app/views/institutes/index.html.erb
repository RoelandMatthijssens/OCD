<h1>All Institutes</h1>
<%= link_to "Add a new institute", new_institute_path %>
<%= will_paginate %>
<ul class="institutes">
  <% @institutes.each do |institute| %>
    <%= render institute %>
  <% end %>
</ul>
<%= will_paginate %>

